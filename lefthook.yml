pre-commit:
  commands:
    autobump:
      run: cargo -Zscript ./scripts/bump-version.rs && git add Cargo.toml Cargo.lock
    check:
      run: ./scripts/check.sh && ./scripts/stage-formatted-rust.sh
